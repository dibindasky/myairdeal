import 'package:flutter/material.dart';

class GoTOHOmeScreen extends StatelessWidget {
  const GoTOHOmeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
      children: [Image.asset('asset/image/plane_image.png')],
    ));
  }
}
